import { createGlobalStyle } from 'styled-components';

export default createGlobalStyle`
    @font-face {
        font-family: 'Popins';
        src: local('Popins'), url(../fonts/Poppins/Poppins-Regular.ttf) format('truetype');
    }
`;